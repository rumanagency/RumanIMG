/* Ruman IMG — Admin Scripts */
( function ( $, params ) {
	'use strict';

	$( document ).ready( function () {
		// Admin JS goes here.
		// params.ajax_url and params.nonce are available.
	} );

} )( jQuery, rumanimg_admin );
